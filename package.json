{
  "name": "t2c",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "T2C — Time to Compute. AI infrastructure delivery intelligence.",
  "scripts": {
    "build": "node build.js",
    "test": "node --test",
    "check": "node --check src/app.js && node build.js",
    "dev": "node build.js && node serve.js",
    "shots": "node scripts/shots.js",
    "interact": "node scripts/interact.js",
    "//overflow": "samples the widths shots.js does not, and names the box that overflows rather than just reporting that one does",
    "overflow": "node scripts/overflow.js",
    "//live": "checks the deployment, not the build: the 404, security headers and the provider-backed routes local QA stubs out",
    "live": "node scripts/live.js",
    "//verify": "build first: the shell tests read dist/, and build.js already gates itself on data validation",
    "verify": "npm run build && npm run test && node scripts/audit.js && node scripts/anchors.js",
    "verify:visual": "npm run verify && npm run shots && npm run overflow && npm run interact"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "puppeteer-core": "^25.7.0",
    "sharp": "^0.35.3"
  }
}
